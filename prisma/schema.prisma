// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id         Int     @id @default(autoincrement())
  title      String
  content    String
  account_id Int
  account    Account @relation(name: "AccountToPost", fields: [account_id], references: [id])

  @@map("posts")
}

model Account {
  id       Int    @id @default(autoincrement())
  posts    Post[] @relation(name: "AccountToPost")
  username String
  email    String
  status   String
}

model Author {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  firstName String
  lastName  String
  books     Book[]   @relation(name: "AuthorToBook")
}

model Book {
  id            Int      @id @default(autoincrement())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  title         String
  isFiction     Boolean
  datePublished DateTime
  author        Author   @relation(name: "AuthorToBook", fields: [authorId], references: [id], onDelete: Cascade)
  authorId      Int
}
